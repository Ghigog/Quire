rootProject.name = "quire-attribution-bakeoff"

// The candidates are scored through the code that ships, never a copy of it: a bake-off
// that measures a reimplementation measures the reimplementation. CLAUDE.md §9.
includeBuild("../..")
